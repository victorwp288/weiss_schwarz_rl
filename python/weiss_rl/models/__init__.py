"""Policy/value model implementation packages.

The stable public import surface is ``weiss_rl.model``. Direct owner imports
live under concept packages: ``policy``, ``backbone``, ``observations``,
``actions``, ``heads``, ``scoring``, and ``public_heuristic``.
"""
