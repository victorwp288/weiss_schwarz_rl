minimal_loop:
  mode: fast
  num_envs: 2
  num_steps: 300
  seed: 7
  max_decisions: 480
  max_ticks: 100000
  observation_visibility: public
  action_policy: uniform_legal
  log_every: 30
  error_policy: replace
  auto_reset_done: true
  auto_reset_engine_errors: true
