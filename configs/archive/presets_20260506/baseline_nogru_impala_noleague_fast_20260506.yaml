schema_version: 2
description: fast no-GRU IMPALA no-league baseline for old-sim thesis comparison
extends: ../../presets/baselines/norecurrence_impala.yaml
system:
  collection_backend: auto
league:
  enabled: false
evaluation:
  periodic_dev_eval_interval_updates: 25
  periodic_dev_eval_paired_seeds: 8
  final_policy_set_selection:
    fixed_anchor_set_v1:
      required:
        - B0 RandomLegal
      optional_if_available:
        - B2 HeuristicPublic
