schema_version: 2
description: eval surface for the combined thesis-model teacher-fade + no-tactical server preset
extends: structured_acceptance_thesis_model_teacher_fade_no_tactical_bias_server_train_auto_gpu.yaml
evaluation:
  final_policy_set_selection:
    fixed_anchor_set_v1:
      optional_if_available:
        - B2 HeuristicPublic
        - B3 HeuristicPublicAggro
        - B4 HeuristicPublicControl
        - Previous champion snapshot
        - Previous recent snapshot
