schema_version: 2
description: structured_acceptance local preset
extends: typed_structured_v2.yaml
system:
  actor_torch_threads: 16
  learner_torch_threads: 16
training:
  profile_timers: false
  structured_metrics:
    mode: sampled
  teacher_aux:
    mode: always
  fixed_opponent_backend: python_batched
