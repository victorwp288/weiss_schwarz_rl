schema_version: 2
extends: ../typed_thesis_locked.yaml
description: "Baseline non-recurrent IMPALA preset"
experiment:
  role: baseline_norecurrence
model:
  encoder_kind: mlp
  recurrent_core: none
training:
  algorithm: impala_vtrace_ff
