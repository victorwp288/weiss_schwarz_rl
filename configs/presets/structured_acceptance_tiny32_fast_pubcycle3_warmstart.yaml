schema_version: 2
description: structured_acceptance tiny32 fast packed WSL frontier with cyclic public teacher during warmstart only
extends: structured_acceptance_tiny32_fast_packed.yaml
training:
  structured_warmstart:
    teacher_public_heuristic_profiles:
      - base
      - aggressive
      - control
    teacher_public_heuristic_profile_mode: cycle
