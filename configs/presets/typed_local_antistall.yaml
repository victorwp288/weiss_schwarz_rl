schema_version: 2
extends: typed_local.yaml
description: "Typed GRU local anti-stall preset with stronger timeout pressure and no-progress curriculum"
rewards:
  shaping:
    damage_reward: 0.02
    no_progress_penalty: 0.01
  truncation:
    reward: -0.5
curriculum:
  simulator:
    max_no_progress_decisions: 96
