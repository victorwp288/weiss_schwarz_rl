schema_version: 2
description: canonical shipping structured_acceptance training preset with automatic multi-GPU actor sharding for Linux servers
extends: structured_acceptance_standard.yaml
system:
  learner_device: cuda:auto
  actor_device: cuda:auto
  collection_backend: process
