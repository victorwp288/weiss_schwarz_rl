schema_version: 2
description: canonical structured_acceptance thesis-eval preset with CUDA eval inference; refuses CPU fallback in eval runners
extends: structured_acceptance_standard_thesis_eval.yaml
evaluation:
  eval_device: cuda:auto
