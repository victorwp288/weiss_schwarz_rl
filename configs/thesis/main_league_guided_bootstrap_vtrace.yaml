schema_version: 2
description: "Guarded main-league bootstrap with conservative V-trace clipping. Use after B1-local search stalls when league bootstrap shows large off-policy correction tails."
extends: main_league_guided_bootstrap.yaml
training:
  vtrace:
    rho_bar: 1.0
    c_bar: 1.0
