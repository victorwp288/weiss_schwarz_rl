schema_version: 2
description: "Selected-a015 exact paired-flip BC probe with old-hard-negative ecology focus. The first paired-flip BC u1 run repaired the B3/B4 fixed surface and newer learned rows, but lost one paired win each to policy000002, checkpoint000025, and bestresponse. This variant keeps the same low-pressure exact-flip replay correction while shifting a little online exposure from champions into the hard-negative lane and focusing the known old regressors. It remains a u1/u2 filter only; continue only if paired confirm shows fixed >= 0, learned > 0, and no old hard-negative row drop versus selected-a015."
extends: main_league_champion_hardneg_selected_paired_flipbc_conservative_probe.yaml
league:
  sampling:
    champion_mix_fraction: 0.14
    hard_negative_mix_fraction: 0.16
    hard_negative_focus_policy_ids:
      - seed_c3aac2f9dc_policy_000002
      - seed_c3aac2f9dc_checkpoint_000025
      - seed_c3aac2f9dc_main_bestresponse_u25_devbest
    hard_negative_focus_weight_multiplier: 3.0
