schema_version: 2
description: "Main league probe: p2 trust-region B1-only continuation with warmup snapshot sampling disabled, keeping opponent exposure on the canonical locked B1 baseline id."
extends: main_b1only_p2_trust_region_probe.yaml
league:
  sampling:
    warmup_snapshot_mix_fraction: 0.0
