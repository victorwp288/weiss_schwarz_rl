# Guided Teacher Config Stack

Internal guided-teacher config fragments used by `configs/thesis/b1_guided_seed.yaml`
and historical guided B1/main probe configs.

These files are not public launch targets. Use package CLI workflows or the
small public configs in `configs/thesis/` and `configs/thesis/ablations/`.
