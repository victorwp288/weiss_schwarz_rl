schema_version: 2
description: "Base-frozen opponent-context candidate-residual top-other probe from a075. Uses the broad current-state conflict-filtered paired-swing rows, but compares each positive action against the current best other legal candidate so the offline gate can verify top-action movement before sentinel eval."
extends: main_league_champion_hardneg_a075_context_candres_broad_conflictfilter_episodepref_probe.yaml
model:
  opponent_context_adapter_lr_multiplier: 2500.0
training:
  structured_aux:
    paired_swing_loss_scope: row
    paired_swing_compare_to: top_other
    paired_swing_aux_updates: 8
    paired_swing_coef: 0.08
    paired_swing_margin: 0.02
